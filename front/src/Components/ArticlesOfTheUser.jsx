import { useEffect, useState } from "react";


export default function ArticlesOfTheUser() {
  const [articles, setArticles] = useState([]);

  const displayArticles = async () => {
    try {
      const response = await fetch 
    }
  }
  return (
    <div>

    </div>
  )
}